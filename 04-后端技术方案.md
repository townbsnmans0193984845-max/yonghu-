# 04 - 后端技术方案

> **文档版本**: v1.0 MVP
> **开发框架**: Node.js + NestJS/Express + TypeScript + Prisma
> **数据库**: PostgreSQL 14+ + Redis 7+

---

## 📋 目录

1. [技术栈](#技术栈)
2. [项目结构](#项目结构)
3. [数据库设计](#数据库设计)
4. [API接口设计](#api接口设计)
5. [业务逻辑实现](#业务逻辑实现)
6. [认证授权](#认证授权)
7. [错误处理](#错误处理)
8. [部署配置](#部署配置)

---

## 技术栈

### 核心框架

```json
{
  "运行时": "Node.js 18 LTS",
  "框架": "NestJS 10.x (推荐) 或 Express 4.x",
  "语言": "TypeScript 5.0+",
  "ORM": "Prisma 5.x",
  "验证": "class-validator + class-transformer"
}
```

### 数据库

```json
{
  "主数据库": "PostgreSQL 14+",
  "缓存": "Redis 7+",
  "连接池": "pg"
}
```

### 认证安全

```json
{
  "认证": "JWT (jsonwebtoken)",
  "加密": "bcrypt (密码/敏感数据)",
  "验证": "joi",
  "限流": "express-rate-limit"
}
```

### 工具库

```json
{
  "日志": "winston + morgan",
  "测试": "jest + supertest",
  "文档": "swagger/openapi",
  "任务调度": "node-cron"
}
```

---

## 项目结构

### NestJS 推荐结构

```
apps/api/
├── src/
│   ├── modules/
│   │   ├── auth/               # 认证模块
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── dto/
│   │   │   └── strategies/
│   │   │
│   │   ├── users/              # 用户模块
│   │   │   ├── users.controller.ts
│   │   │   ├── users.service.ts
│   │   │   ├── users.module.ts
│   │   │   └── dto/
│   │   │
│   │   ├── jobs/               # 岗位模块
│   │   │   ├── jobs.controller.ts
│   │   │   ├── jobs.service.ts
│   │   │   ├── jobs.module.ts
│   │   │   └── dto/
│   │   │
│   │   ├── applications/       # 报名模块
│   │   │   ├── applications.controller.ts
│   │   │   ├── applications.service.ts
│   │   │   ├── applications.module.ts
│   │   │   └── dto/
│   │   │
│   │   └── admin/              # 管理后台模块
│   │       ├── admin.controller.ts
│   │       ├── admin.service.ts
│   │       └── admin.module.ts
│   │
│   ├── common/                 # 公共模块
│   │   ├── filters/            # 异常过滤器
│   │   ├── interceptors/       # 拦截器
│   │   ├── pipes/              # 管道
│   │   ├── guards/             # 守卫
│   │   ├── decorators/         # 装饰器
│   │   └── utils/              # 工具函数
│   │
│   ├── config/                 # 配置
│   │   ├── database.config.ts
│   │   ├── jwt.config.ts
│   │   └── redis.config.ts
│   │
│   ├── database/               # 数据库
│   │   ├── prisma/
│   │   │   └── schema.prisma
│   │   └── migrations/
│   │
│   ├── main.ts                 # 入口文件
│   └── app.module.ts           # 根模块
│
├── test/                       # 测试
├── .env.example
├── nest-cli.json
├── tsconfig.json
└── package.json
```

---

## 数据库设计

### Prisma Schema

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 用户表
model User {
  id           String    @id @default(uuid())
  phone        String    @unique
  role         Role      @default(worker)
  name         String?
  credit_score Int       @default(100)
  created_at   DateTime  @default(now())
  updated_at   DateTime  @updatedAt

  // 关联
  postedJobs       Job[]          @relation("RecruiterJobs")
  applications     Application[]  @relation("WorkerApplications")
  loginLogs        LoginLog[]

  @@map("users")
}

// 岗位表
model Job {
  id                String   @id @default(uuid())
  recruiter_id      String
  title             String
  description       String   @db.Text
  salary            Decimal  @db.Decimal(10, 2)
  salary_unit       SalaryUnit @default(hour)
  location          String
  required_workers  Int
  current_applications Int   @default(0)
  status            JobStatus @default(open)
  created_at        DateTime @default(now())
  updated_at        DateTime @updatedAt

  // 关联
  recruiter     User          @relation("RecruiterJobs", fields: [recruiter_id], references: [id])
  applications  Application[]

  @@map("jobs")
}

// 报名表
model Application {
  id             String           @id @default(uuid())
  job_id         String
  worker_id      String
  status         ApplicationStatus @default(pending)
  applied_date   DateTime         @default(now())
  check_in_time  DateTime?
  check_out_time DateTime?
  created_at     DateTime         @default(now())
  updated_at     DateTime         @updatedAt

  // 关联
  job    Job @relation(fields: [job_id], references: [id])
  worker User @relation("WorkerApplications", fields: [worker_id], references: [id])

  @@unique([job_id, worker_id])
  @@map("applications")
}

// 登录日志
model LoginLog {
  id         String   @id @default(uuid())
  user_id    String
  ip         String?
  user_agent String?
  created_at DateTime @default(now())

  // 关联
  user User @relation(fields: [user_id], references: [id])

  @@map("login_logs")
}

// 枚举定义
enum Role {
  worker
  recruiter
  admin
}

enum SalaryUnit {
  hour
  day
  time
}

enum JobStatus {
  open
  closed
}

enum ApplicationStatus {
  pending       // 待审核
  accepted      // 已录用
  rejected      // 已拒绝
  in_progress   // 工作中
  completed     // 已完工
  settled       // 已结算
}
```

### 数据库初始化SQL

```sql
-- 创建数据库
CREATE DATABASE timecoin_job;

-- 创建索引
CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_jobs_recruiter ON jobs(recruiter_id);
CREATE INDEX idx_jobs_status ON jobs(status);
CREATE INDEX idx_jobs_created ON jobs(created_at DESC);
CREATE INDEX idx_applications_job ON applications(job_id);
CREATE INDEX idx_applications_worker ON applications(worker_id);
CREATE INDEX idx_applications_status ON applications(status);
```

---

## API接口设计

### 统一响应格式

```typescript
// 成功响应
{
  "code": 200,
  "message": "success",
  "data": { ... }
}

// 错误响应
{
  "code": 400,
  "message": "参数错误",
  "error": { ... }
}

// 分页响应
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [ ... ],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

### 认证模块 API

```typescript
// POST /api/v1/auth/send-code
// 发送验证码
Request:
{
  "phone": "13800138000"
}

Response:
{
  "code": 200,
  "message": "验证码已发送"
}

// POST /api/v1/auth/login
// 登录
Request:
{
  "phone": "13800138000",
  "code": "123456"
}

Response:
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": "user_123",
      "phone": "13800138000",
      "role": "worker",
      "name": "张三",
      "credit_score": 95
    }
  }
}

// GET /api/v1/auth/me
// 获取当前用户信息
Headers:
  Authorization: Bearer {token}

Response:
{
  "code": 200,
  "data": {
    "id": "user_123",
    "phone": "13800138000",
    "role": "worker",
    "name": "张三",
    "credit_score": 95
  }
}
```

### 岗位模块 API

```typescript
// GET /api/v1/jobs
// 获取岗位列表
Query Params:
  keyword?: string    // 关键字搜索
  city?: string       // 城市筛选
  page?: number       // 页码,默认1
  pageSize?: number   // 每页数量,默认20

Response:
{
  "code": 200,
  "data": {
    "items": [
      {
        "id": "job_123",
        "title": "超市促销员",
        "description": "负责超市内商品推广",
        "salary": "50.00",
        "salary_unit": "hour",
        "location": "北京市朝阳区",
        "required_workers": 5,
        "current_applications": 3,
        "status": "open",
        "recruiter": {
          "id": "user_456",
          "name": "XX超市",
          "credit_score": 95
        },
        "created_at": "2026-02-05T10:00:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}

// GET /api/v1/jobs/:id
// 获取岗位详情
Response:
{
  "code": 200,
  "data": {
    "id": "job_123",
    "title": "超市促销员",
    "description": "负责超市内商品推广...",
    "salary": "50.00",
    "salary_unit": "hour",
    "location": "北京市朝阳区XX路XX号",
    "required_workers": 5,
    "current_applications": 3,
    "recruiter": {
      "name": "XX超市",
      "phone": "138****1234",
      "credit_score": 95
    },
    "created_at": "2026-02-05T10:00:00Z"
  }
}

// POST /api/v1/jobs
// 创建岗位(仅招聘方)
Headers:
  Authorization: Bearer {token}

Request:
{
  "title": "超市促销员",
  "description": "负责超市内商品推广",
  "salary": 50,
  "salary_unit": "hour",
  "location": "北京市朝阳区",
  "required_workers": 5
}

Response:
{
  "code": 201,
  "message": "发布成功",
  "data": {
    "id": "job_123",
    "title": "超市促销员",
    "status": "open",
    ...
  }
}
```

### 报名模块 API

```typescript
// POST /api/v1/jobs/:jobId/apply
// 报名(仅兼职者)
Headers:
  Authorization: Bearer {token}

Response:
{
  "code": 201,
  "message": "报名成功",
  "data": {
    "id": "app_123",
    "status": "pending",
    "job_id": "job_123",
    "wallet": {
      "available_balance": 80,
      "frozen_balance": 20
    }
  }
}

// GET /api/v1/applications/me
// 获取我的报名(仅兼职者)
Query Params:
  status?: string  // 状态筛选

Response:
{
  "code": 200,
  "data": [
    {
      "id": "app_123",
      "status": "accepted",
      "applied_date": "2026-02-05T11:00:00Z",
      "job": {
        "id": "job_123",
        "title": "超市促销员",
        "salary": "50.00",
        "location": "北京市朝阳区"
      }
    }
  ]
}

// POST /api/v1/applications/:id/accept
// 录用(仅招聘方)
Response:
{
  "code": 200,
  "message": "录用成功",
  "data": {
    "id": "app_123",
    "status": "accepted"
  }
}

// POST /api/v1/applications/:id/reject
// 拒绝(仅招聘方)
Response:
{
  "code": 200,
  "message": "已拒绝",
  "data": {
    "id": "app_123",
    "status": "rejected",
    "wallet": {
      "available_balance": 100,
      "frozen_balance": 0
    }
  }
}

// POST /api/v1/applications/:id/check-in
// 上岗打卡(仅兼职者)
Response:
{
  "code": 200,
  "message": "打卡成功",
  "data": {
    "id": "app_123",
    "status": "in_progress",
    "check_in_time": "2026-02-05T14:00:00Z"
  }
}

// POST /api/v1/applications/:id/complete
// 确认完工(仅招聘方)
Response:
{
  "code": 200,
  "message": "结算完成",
  "data": {
    "id": "app_123",
    "status": "settled",
    "settlement": {
      "salary": 200,
      "deposit_refunded": 20,
      "total": 220
    }
  }
}
```

---

## 业务逻辑实现

### 报名逻辑(事务)

```typescript
// applications.service.ts
async apply(jobId: string, workerId: string) {
  // 1. 开启事务
  return await this.prisma.$transaction(async (tx) => {
    // 2. 校验岗位是否可报名
    const job = await tx.job.findUnique({
      where: { id: jobId },
    });

    if (!job || job.status !== JobStatus.OPEN) {
      throw new BadRequestException('岗位不存在或已关闭');
    }

    if (job.current_applications >= job.required_workers) {
      throw new BadRequestException('名额已满');
    }

    // 3. 校验是否已报名
    const existing = await tx.application.findUnique({
      where: {
        job_id_worker_id: {
          job_id: jobId,
          worker_id: workerId,
        },
      },
    });

    if (existing) {
      throw new BadRequestException('已报名过该岗位');
    }

    // 4. 计算押金
    const deposit = Number(job.salary) * 0.2;

    // 5. 校验余额
    const worker = await tx.user.findUnique({
      where: { id: workerId },
    });

    // TODO: 检查虚拟余额是否足够

    // 6. 冻结押金
    // TODO: 更新钱包余额

    // 7. 创建报名记录
    const application = await tx.application.create({
      data: {
        job_id: jobId,
        worker_id: workerId,
        status: ApplicationStatus.PENDING,
      },
    });

    // 8. 更新岗位报名人数
    await tx.job.update({
      where: { id: jobId },
      data: {
        current_applications: {
          increment: 1,
        },
      },
    });

    return application;
  });
}
```

### 结算逻辑(事务)

```typescript
async settle(applicationId: string) {
  return await this.prisma.$transaction(async (tx) => {
    // 1. 获取报名信息
    const application = await tx.application.findUnique({
      where: { id: applicationId },
      include: {
        job: true,
        worker: true,
      },
    });

    if (!application || application.status !== ApplicationStatus.COMPLETED) {
      throw new BadRequestException('状态不允许结算');
    }

    // 2. 计算工作时长和薪资
    const checkIn = new Date(application.check_in_time!);
    const checkOut = new Date(application.check_out_time!);
    const workHours = (checkOut.getTime() - checkIn.getTime()) / (1000 * 60 * 60);
    const salary = Number(application.job.salary) * workHours;

    // 3. 计算押金退款
    const deposit = Number(application.job.salary) * 0.2;
    const total = salary + deposit;

    // 4. 发放薪资
    // TODO: 增加兼职者虚拟余额

    // 5. 退还押金
    // TODO: 解冻押金

    // 6. 更新报名状态
    await tx.application.update({
      where: { id: applicationId },
      data: {
        status: ApplicationStatus.SETTLED,
      },
    });

    return {
      salary,
      deposit_refunded: deposit,
      total,
    };
  });
}
```

---

## 认证授权

### JWT配置

```typescript
// src/config/jwt.config.ts
export const jwtConfig = {
  secret: process.env.JWT_SECRET || 'your-secret-key',
  expiresIn: '7d',
  refreshExpiresIn: '30d',
};
```

### JWT策略

```typescript
// src/modules/auth/strategies/jwt.strategy.ts
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor() {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: jwtConfig.secret,
    });
  }

  async validate(payload: any) {
    if (!payload.sub) {
      throw new UnauthorizedException();
    }
    return { userId: payload.sub, role: payload.role };
  }
}
```

### 角色守卫

```typescript
// src/common/guards/roles.guard.ts
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { Role } from '@prisma/client';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private readonly allowedRoles: Role[]) {}

  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest();
    const user = request.user;

    if (!user) {
      return false;
    }

    return this.allowedRoles.includes(user.role);
  }
}

// 使用装饰器
export const RequireRoles = (...roles: Role[]) =>
  SetMetadata('roles', roles);
```

---

## 错误处理

### 统一错误码

```typescript
// src/common/constants/error-codes.ts
export enum ErrorCode {
  // 通用错误 1xxx
  UNAUTHORIZED = 1001,
  FORBIDDEN = 1002,
  NOT_FOUND = 1003,
  INVALID_PARAMS = 1004,

  // 认证错误 2xxx
  USER_NOT_FOUND = 2001,
  INVALID_CODE = 2002,
  PHONE_EXISTS = 2003,

  // 业务错误 3xxx
  JOB_CLOSED = 3001,
  ALREADY_APPLIED = 3002,
  INSUFFICIENT_BALANCE = 3003,
  INVALID_STATUS = 3004,
}

export const ErrorMessages: Record<ErrorCode, string> = {
  [ErrorCode.UNAUTHORIZED]: '未登录或登录已过期',
  [ErrorCode.FORBIDDEN]: '无权限访问',
  [ErrorCode.NOT_FOUND]: '资源不存在',
  [ErrorCode.INVALID_PARAMS]: '参数错误',
  [ErrorCode.USER_NOT_FOUND]: '用户不存在',
  [ErrorCode.INVALID_CODE]: '验证码错误',
  [ErrorCode.PHONE_EXISTS]: '手机号已注册',
  [ErrorCode.JOB_CLOSED]: '岗位已关闭',
  [ErrorCode.ALREADY_APPLIED]: '已报名过该岗位',
  [ErrorCode.INSUFFICIENT_BALANCE]: '余额不足',
  [ErrorCode.INVALID_STATUS]: '状态不允许该操作',
};
```

### 异常过滤器

```typescript
// src/common/filters/all-exceptions.filter.ts
import {
  ExceptionFilter,
  Catch,
  ArgumentsHost,
  HttpException,
  HttpStatus,
} from '@nestjs/common';
import { Response } from 'express';

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    let status = HttpStatus.INTERNAL_SERVER_ERROR;
    let message = '服务器内部错误';
    let code = 500;

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      code = status;
      message = exception.message;
    }

    response.status(status).json({
      code,
      message,
      timestamp: new Date().toISOString(),
    });
  }
}
```

---

## 部署配置

### Docker配置

```dockerfile
# Dockerfile
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
COPY prisma ./prisma/
RUN npm ci
COPY . .
RUN npm run build

FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package*.json ./
EXPOSE 3000
CMD ["npm", "run", "start:prod"]
```

### Docker Compose

```yaml
# docker-compose.yml
version: '3.8'
services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://user:pass@postgres:5432/timecoin
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=your-secret-key
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:14-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=timecoin
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

---

**下一篇: `附录/API接口字典.md`** 📖
