# 职兼兼职兼平台 - 技术开发文档总览

> **文档版本**: v1.0 MVP
> **交付对象**: 前端、后端、测试工程师
> **文档目标**: 开发团队可直接基于此文档进行开发

---

## 📦 文档包结构

本文档包包含以下内容，开发团队应按顺序阅读：

```
【070】职兼兼saas/
├── 01-产品需求文档.md              # 产品需求、业务流程、功能清单
├── 02-UI UX设计规范.md              # 原型图、设计规范、交互说明
├── 03-前端技术方案.md               # 前端架构、组件设计、状态管理
├── 04-后端技术方案.md               # 后端架构、API设计、数据库设计
├── 05-管理后台需求.md               # 后台管理系统详细需求
├── 06-部署运维文档.md               # 部署架构、运维监控、安全配置
├── 07-测试验收标准.md               # 测试用例、验收标准、性能指标
└── 附录/
    ├── API接口字典.md               # 所有API接口完整定义
    ├── 数据库表结构SQL.md           # 完整的DDL脚本
    └── 错误码定义.md                 # 统一错误码规范
```

---

## 🎯 MVP 功能范围

### 包含功能（v1.0）

#### 1. 用户系统
- 手机号+验证码登录注册
- 身份选择（兼职者/招聘方）
- 个人资料管理
- JWT 认证授权

#### 2. 招聘方功能
- 发布岗位（标题、描述、薪资、地点、人数）
- 查看报名列表
- 审核报名（录用/拒绝）
- 确认完工

#### 3. 兼职者功能
- 浏览岗位列表（搜索、筛选）
- 查看岗位详情
- 报名（简化押金逻辑：报名时冻结虚拟押金）
- 查看我的报名
- 上岗/下岗打卡

#### 4. 状态流转
```
报名 → pending
    ↓
招聘方审核 → accepted / rejected
    ↓ (accepted)
上岗打卡 → in_progress
    ↓
下岗打卡 → completed
    ↓
招聘方确认 → settled (自动结算)
```

#### 5. 管理后台
- 用户管理（查看、封禁）
- 岗位管理（审核、下架）
- 报名监控
- 数据统计

### 不包含功能（预留 v2.0）

- ❌ 真实支付（支付宝/微信支付集成）
- ❌ 钱包系统（充值、提现）
- ❌ 实时聊天（WebSocket IM）
- ❌ 争议仲裁系统
- ❌ 信用分算法
- ❌ 地图推荐
- ❌ 消息推送

---

## 🛠️ 技术栈

### 前端

```json
{
  "框架": "React 18.2+",
  "构建工具": "Vite 4.x",
  "UI框架": "Ant Design 5.x (PC端) / Ant Design Mobile 5.x (H5)",
  "状态管理": "Zustand 4.x",
  "数据请求": "Axios 1.x + React Query 5.x",
  "路由": "React Router 6.x",
  "表单": "React Hook Form 7.x + Zod 3.x",
  "样式": "CSS Modules + Tailwind CSS",
  "类型检查": "TypeScript 5.x"
}
```

### 后端

```json
{
  "运行时": "Node.js 18 LTS",
  "框架": "Express 4.x / NestJS 10.x (推荐)",
  "语言": "TypeScript 5.x",
  "ORM": "Prisma 5.x",
  "数据库": "PostgreSQL 14+",
  "缓存": "Redis 7+",
  "认证": "jsonwebtoken + bcrypt",
  "验证": "Zod / class-validator",
  "文档": "Swagger/OpenAPI 3.0",
  "日志": "Winston + Morgan"
}
```

### 基础设施

```json
{
  "容器化": "Docker + Docker Compose",
  "反向代理": "Nginx",
  "进程管理": "PM2",
  "监控": "Prometheus + Grafana (可选)",
  "日志收集": "ELK Stack (可选)",
  "CI/CD": "GitHub Actions / GitLab CI"
}
```

---

## 📊 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                        用户层                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 兼职者H5 │  │招聘方H5  │  │ 管理后台 │              │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘              │
└────────┼─────────────┼─────────────┼──────────────────┘
         │             │             │
         └─────────────┼─────────────┘
                       │
         ┌─────────────▼─────────────┐
         │      API Gateway / BFF    │  (鉴权、限流、路由)
         └─────────────┬─────────────┘
                       │
    ┌──────────────────┼──────────────────┐
    │                  │                  │
┌───▼───┐      ┌──────▼──────┐    ┌─────▼─────┐
│ Auth  │      │   Job Service│    │Application│
│Service│      │  (岗位管理)   │    │ Service   │
└───┬───┘      └──────┬──────┘    └─────┬─────┘
    │                 │                  │
    └─────────────────┼──────────────────┘
                      │
         ┌────────────▼────────────┐
         │    PostgreSQL Database  │
         └────────────┬────────────┘
                      │
         ┌────────────▼────────────┐
         │       Redis Cache       │
         └─────────────────────────┘
```

### 数据流

```
用户请求 → API Gateway → 鉴权验证 → 业务逻辑处理 → 数据库操作
                                      ↓
                                 缓存更新(Redis)
                                      ↓
                                 返回响应
```

---

## 📋 数据库设计概览

### 核心数据表

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `users` | 用户表 | id, phone, role, name, created_at |
| `jobs` | 岗位表 | id, recruiter_id, title, salary, status |
| `applications` | 报名表 | id, job_id, worker_id, status, check_in_time |
| `login_logs` | 登录日志 | id, user_id, ip, user_agent, created_at |

详细表结构见：`附录/数据库表结构SQL.md`

---

## 🔌 API 设计概览

### API 风格

- **RESTful API**
- 统一前缀：`/api/v1`
- 认证方式：`Bearer Token` (JWT)
- 响应格式：JSON

### 核心接口

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 认证 | `/auth/register` | POST | 注册 |
| 认证 | `/auth/login` | POST | 登录 |
| 岗位 | `/jobs` | GET | 获取岗位列表 |
| 岗位 | `/jobs/:id` | GET | 获取岗位详情 |
| 岗位 | `/jobs` | POST | 创建岗位(招聘方) |
| 报名 | `/applications` | POST | 报名(兼职者) |
| 报名 | `/applications/:id/accept` | POST | 录用(招聘方) |
| 报名 | `/applications/:id/check-in` | POST | 上岗打卡 |
| 报名 | `/applications/:id/complete` | POST | 确认完工 |

详细接口定义见：`附录/API接口字典.md`

---

## 🎨 前端页面结构

### 页面清单

#### 兼职者端
1. `login` - 登录页
2. `register` - 注册页
3. `role-select` - 身份选择
4. `worker/home` - 首页(岗位列表)
5. `worker/job-detail` - 岗位详情
6. `worker/my-applications` - 我的报名
7. `worker/profile` - 个人中心

#### 招聘方端
1. `recruiter/home` - 首页(岗位管理)
2. `recruiter/post-job` - 发布岗位
3. `recruiter/applications` - 报名管理
4. `recruiter/profile` - 个人中心

#### 管理后台
1. `admin/dashboard` - 数据看板
2. `admin/users` - 用户管理
3. `admin/jobs` - 岗位管理
4. `admin/applications` - 报名监控

详细页面设计见：`02-UI UX设计规范.md`

---

## 📐 开发规范

### 代码规范

- **命名规范**：camelCase (JS/TS), snake_case (数据库)
- **注释规范**：JSDoc (函数/类), 关键业务逻辑必须注释
- **Git规范**：Conventional Commits (`feat:`, `fix:`, `refactor:`)

### 接口规范

- 统一响应格式
- 统一错误码
- 接口版本控制 (`/api/v1`)

### 数据库规范

- 所有表必须有：`id`, `created_at`, `updated_at`
- 软删除使用 `deleted_at`
- 外键命名：`{table}_id`

---

## 🚀 开发流程

### 阶段划分

| 阶段 | 工作内容 | 交付物 | 预估时间 |
|------|----------|--------|----------|
| **阶段0** | 环境搭建 | 项目脚手架、数据库初始化 | 2天 |
| **阶段1** | 数据库+基础API | 数据表、认证API、用户API | 3天 |
| **阶段2** | 岗位模块 | 岗位CRUD、搜索筛选 | 3天 |
| **阶段3** | 报名模块 | 报名、审核、状态流转 | 4天 |
| **阶段4** | 打卡结算 | 打卡API、结算逻辑 | 3天 |
| **阶段5** | 前端开发 | 所有页面、交互逻辑 | 5天 |
| **阶段6** | 管理后台 | 后台功能开发 | 3天 |
| **阶段7** | 联调测试 | 端到端测试、bug修复 | 3天 |
| **阶段8** | 部署上线 | 生产环境部署、监控配置 | 2天 |

**总计**: 约 4-5 周（1人全职）或 2-3 周（2-3人团队）

---

## ✅ 验收标准

### 功能验收

- [ ] 用户可以注册登录
- [ ] 招聘方可以发布岗位
- [ ] 兼职者可以浏览和报名
- [ ] 招聘方可以审核报名
- [ ] 兼职者可以打卡
- [ ] 状态流转正确
- [ ] 管理员可以管理用户和岗位

### 性能验收

- [ ] 核心接口响应时间 < 300ms (P95)
- [ ] 页面首屏加载 < 2s
- [ ] 支持 100 并发用户

### 安全验收

- [ ] JWT 认证正确实现
- [ ] SQL注入防护
- [ ] XSS 防护
- [ ] 敏感数据加密存储

详细测试用例见：`07-测试验收标准.md`

---

## 📞 文档维护

### 版本历史

| 版本 | 日期 | 变更说明 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-02-05 | MVP版本初始文档 | Claude |

### 问题反馈

如文档有疑问或不清晰之处，请联系：
- 产品负责人：[待填写]
- 技术负责人：[待填写]

---

## 📝 使用说明

### 给前端工程师

1. 先阅读 `01-产品需求文档.md` 了解业务
2. 再阅读 `02-UI UX设计规范.md` 了解UI
3. 然后阅读 `03-前端技术方案.md` 了解技术实现
4. 参考 `附录/API接口字典.md` 进行对接

### 给后端工程师

1. 先阅读 `01-产品需求文档.md` 了解业务
2. 再阅读 `04-后端技术方案.md` 了解架构
3. 参考 `附录/数据库表结构SQL.md` 初始化数据库
4. 按照 `附录/API接口字典.md` 实现接口

### 给测试工程师

1. 阅读 `01-产品需求文档.md` 了解业务
2. 参考 `07-测试验收标准.md` 编写测试用例

---

**开始阅读第一篇：`01-产品需求文档.md`** 📖
