# 01 - 产品需求文档 (PRD)

> **文档版本**: v1.0 MVP
> **最后更新**: 2026-02-05
> **产品定位**: 兼职招聘平台 - 即时结算 + 强约束履约

---

## 📋 目录

1. [产品概述](#产品概述)
2. [核心业务流程](#核心业务流程)
3. [角色与权限](#角色与权限)
4. [功能详细需求](#功能详细需求)
5. [状态机设计](#状态机设计)
6. [数据模型概要](#数据模型概要)
7. [非功能需求](#非功能需求)

---

## 产品概述

### 产品愿景

打造一个**即时结算、强约束履约**的兼职招聘平台，解决传统兼职市场的痛点：
- ❌ 薪资拖欠
- ❌ 鸽子率高（放鸽子）
- ❌ 纠纷难处理

### 核心价值

1. **即时结算**: 兼职完工后自动结算薪资，降低劳资摩擦
2. **强约束履约**: 通过押金机制提升到岗率和完工率
3. **流程透明**: 全链路可追踪，减少纠纷

### MVP 范围

#### ✅ v1.0 包含

- 手机号注册登录（验证码简化为固定密码/第三方验证码服务）
- 双身份体系（兼职者/招聘方）
- 岗位发布与浏览
- 报名与审核流程
- 打卡功能（上岗/下岗）
- 简化的结算逻辑（不涉及真实支付）
- 基础管理后台

#### ❌ v2.0 预留

- 真实支付集成（支付宝/微信）
- 钱包系统（充值/提现/流水）
- 实时聊天（WebSocket IM）
- 争议仲裁系统
- 信用分算法
- 地图推荐
- 消息推送（短信/站内信/推送）

---

## 核心业务流程

### 1. 注册登录流程

```
用户 → 打开应用 → 选择身份(兼职者/招聘方)
                ↓
           输入手机号 + 验证码
                ↓
           首次登录补充资料
                ↓
    兼职者: 姓名、技能标签、常驻城市
    招聘方: 企业名称、联系人、职位
                ↓
           进入对应首页
```

### 2. 招聘方发岗流程

```
招聘方 → 点击"发布岗位"
          ↓
     填写岗位信息:
     - 标题、描述
     - 薪资(元/小时或元/天)
     - 工作地点
     - 招聘人数
     - 工作时间
          ↓
     系统校验:
     - 薪资 > 0
     - 人数 > 0
          ↓
     创建岗位(状态=open)
          ↓
     返回岗位列表
```

### 3. 兼职者报名流程

```
兼职者 → 浏览岗位列表 → 点击岗位详情
                              ↓
                         点击"立即报名"
                              ↓
                    系统校验:
                    - 余额是否足够(虚拟押金)
                    - 是否已报名
                              ↓
                    创建报名记录(状态=pending)
                    冻结押金(可用→冻结)
                              ↓
                    提示:报名成功,等待审核
```

### 4. 审核流程

```
招聘方 → 查看报名列表 → 选择候选人
                              ↓
                    点击"录用"或"拒绝"
                              ↓
              ┌───────────────┴───────────────┐
              ↓                               ↓
         录用(accepted)                   拒绝(rejected)
              ↓                               ↓
      兼职者收到通知                    自动解冻押金
      状态变更为"待到岗"                 通知兼职者
```

### 5. 履约与结算流程

```
兼职者(已录用) → 到岗 → 点击"上岗打卡"
                            ↓
                   更新状态: in_progress
                   记录check_in_time
                            ↓
                   完成工作 → 点击"下岗打卡"
                            ↓
                   记录check_out_time
                   更新状态: completed
                            ↓
                   招聘方确认完工
                            ↓
                   ┌──────────┴──────────┐
                   ↓                     ↓
             招聘方同意              招聘方拒绝
                   ↓                     ↓
             自动结算:              进入争议(v2.0)
             1. 发放薪资
             2. 退还押金
             3. 状态→settled
```

---

## 角色与权限

### 角色定义

| 角色 | 代码 | 说明 |
|------|------|------|
| 兼职者 | `worker` | 提供兼职服务的个人 |
| 招聘方 | `recruiter` | 发布兼职需求的企业或个人 |
| 管理员 | `admin` | 平台运营人员 |

### 权限矩阵

| 功能模块 | 兼职者 | 招聘方 | 管理员 |
|----------|--------|--------|--------|
| 注册登录 | ✅ | ✅ | ✅ |
| 浏览岗位 | ✅ | ❌ | ✅ |
| 发布岗位 | ❌ | ✅ | - |
| 报名 | ✅ | ❌ | - |
| 审核报名 | ❌ | ✅ | - |
| 打卡 | ✅ | ❌ | - |
| 确认完工 | ❌ | ✅ | - |
| 查看我的报名 | ✅ | ❌ | - |
| 管理岗位 | ❌ | ✅ | ✅ |
| 管理用户 | ❌ | ❌ | ✅ |
| 封禁账号 | ❌ | ❌ | ✅ |
| 查看统计数据 | ❌ | ❌ | ✅ |

---

## 功能详细需求

### 1. 用户模块

#### 1.1 注册

**功能描述**: 用户通过手机号注册账号

**前置条件**: 无

**输入**:
- 手机号 (11位)
- 验证码 (6位数字)

**业务规则**:
- 手机号格式: `1[3-9]\d{9}`
- 验证码: 暂时简化为固定值 `123456` (生产环境需对接短信服务商)
- 手机号未注册过

**输出**:
- 成功: 返回 JWT Token, 跳转到身份选择页
- 失败: 返回错误码 (`PHONE_EXISTS`, `INVALID_CODE`, `INVALID_PHONE`)

**后续动作**:
- 首次注册需补充个人资料

#### 1.2 登录

**功能描述**: 已注册用户登录

**前置条件**: 手机号已注册

**输入**:
- 手机号
- 验证码

**业务规则**:
- 验证码正确
- 账号状态正常(未被封禁)

**输出**:
- 成功: 返回 JWT Token + 用户信息
- 失败: 返回错误码 (`USER_NOT_FOUND`, `INVALID_CODE`, `USER_BANNED`)

#### 1.3 身份选择

**功能描述**: 首次登录选择身份

**前置条件**: 已登录,角色字段为空

**输入**:
- 角色: `worker` 或 `recruiter`

**业务规则**:
- 身份选定后不可修改(或需客服申请修改)

**输出**:
- 成功: 更新用户角色,跳转到对应首页

#### 1.4 补充资料

**功能描述**: 完善个人信息

**兼职者必填**:
- 姓名 (text)
- 技能标签 (text[], 如: "配送", "服务员", "发传单")
- 常驻城市 (text)

**招聘方必填**:
- 企业/个人名称 (text)
- 联系人 (text)
- 联系电话 (text)

**输出**:
- 成功: 更新用户信息,进入首页

---

### 2. 岗位模块

#### 2.1 发布岗位(招聘方)

**功能描述**: 招聘方发布兼职岗位

**前置条件**:
- 已登录
- 角色为招聘方

**输入**:
- 标题 (text, 必填, 1-50字符)
- 描述 (text, 必填, 1-500字符)
- 薪资 (number, 必填, >0)
- 薪资单位 (option: "小时", "天", "次")
- 工作地点 (text, 必填)
- 招聘人数 (number, 必填, >0)
- 工作时间 (text, 如: "2026-02-10 09:00-18:00")

**业务规则**:
- 薪资必须大于0
- 人数必须大于0
- 标题不能为空

**输出**:
- 成功: 创建岗位记录,状态=`open`
- 失败: 返回字段错误信息

#### 2.2 浏览岗位列表(兼职者)

**功能描述**: 兼职者浏览所有招聘中的岗位

**前置条件**:
- 已登录
- 角色为兼职者

**查询参数**:
- 关键字 (搜索标题)
- 城市 (筛选)
- 薪资范围 (筛选: 0-50, 50-100, 100+)
- 排序 (最新发布 / 薪资最高)
- 分页 (page, pageSize)

**业务规则**:
- 只显示状态为 `open` 的岗位
- 默认按创建时间倒序

**输出**:
```json
{
  "data": [
    {
      "id": "job_123",
      "title": "超市促销员",
      "salary": 50,
      "salary_unit": "小时",
      "location": "北京市朝阳区",
      "required_workers": 5,
      "recruiter": {
        "name": "XX超市",
        "credit_score": 95
      },
      "created_at": "2026-02-05T10:00:00Z"
    }
  ],
  "total": 100,
  "page": 1,
  "pageSize": 20
}
```

#### 2.3 查看岗位详情(兼职者)

**功能描述**: 查看单个岗位的详细信息

**前置条件**:
- 已登录
- 角色为兼职者
- 岗位存在且状态为 `open`

**输出**:
```json
{
  "id": "job_123",
  "title": "超市促销员",
  "description": "负责超市内商品推广...",
  "salary": 50,
  "salary_unit": "小时",
  "location": "北京市朝阳区XX路XX号",
  "required_workers": 5,
  "current_applications": 3,
  "recruiter": {
    "name": "XX超市",
    "phone": "138****1234",
    "credit_score": 95
  },
  "can_apply": true,
  "apply_reason": ""  // 如果不可报名,说明原因
}
```

**显示逻辑**:
- 如果已报名,不显示"立即报名"按钮
- 如果岗位已满,显示"名额已满"
- 如果余额不足,显示"余额不足"并引导充值

---

### 3. 报名模块

#### 3.1 提交报名(兼职者)

**功能描述**: 兼职者报名岗位

**前置条件**:
- 已登录
- 角色为兼职者
- 岗位状态为 `open`
- 未报名过该岗位
- 押金金额(虚拟)=薪资的20%

**业务规则**:
1. 校验岗位是否可报名
2. 校验用户是否已报名
3. 计算押金金额: `deposit = salary * 0.2`
4. 冻结押金: `available_balance -= deposit`, `frozen_balance += deposit`
5. 创建报名记录,状态=`pending`
6. 扣减岗位名额(逻辑上,非物理删除)

**输出**:
```json
{
  "application_id": "app_123",
  "status": "pending",
  "wallet": {
    "available_balance": 80,
    "frozen_balance": 20
  }
}
```

**错误处理**:
- `JOB_CLOSED`: 岗位已关闭
- `ALREADY_APPLIED`: 已报名过
- `INSUFFICIENT_BALANCE`: 余额不足

#### 3.2 查看报名列表(招聘方)

**功能描述**: 招聘方查看某个岗位的所有报名

**前置条件**:
- 已登录
- 角色为招聘方
- 是岗位的发布者

**查询参数**:
- 岗位ID
- 报名状态筛选

**输出**:
```json
{
  "data": [
    {
      "id": "app_123",
      "worker": {
        "name": "张三",
        "phone": "139****5678",
        "credit_score": 98,
        "completed_jobs": 15
      },
      "status": "pending",
      "applied_at": "2026-02-05T11:00:00Z"
    }
  ]
}
```

**排序**:
- 默认按信用分降序
- 其次按报名时间升序

#### 3.3 审核报名(招聘方)

**功能描述**: 招聘方录用或拒绝报名者

**前置条件**:
- 已登录
- 角色为招聘方
- 报名状态为 `pending`

**输入**:
- 报名ID
- 操作: `accept` 或 `reject`

**业务规则**:

**录用 (accept)**:
1. 更新报名状态为 `accepted`
2. 通知兼职者(v2.0实现)
3. 岗位已录取人数+1

**拒绝 (reject)**:
1. 更新报名状态为 `rejected`
2. 自动解冻押金: `frozen_balance -= deposit`, `available_balance += deposit`
3. 通知兼职者(v2.0实现)

**输出**:
- 成功: 返回更新后的报名记录
- 失败: 返回错误码 (`INVALID_STATUS`, `NOT_OWNER`)

---

### 4. 打卡模块

#### 4.1 上岗打卡(兼职者)

**功能描述**: 兼职者到岗后打卡

**前置条件**:
- 已登录
- 角色为兼职者
- 报名状态为 `accepted`

**业务规则**:
1. 校验当前时间是否在岗位工作时间内
2. 更新报名状态为 `in_progress`
3. 记录 `check_in_time` = 当前时间

**输出**:
```json
{
  "application_id": "app_123",
  "status": "in_progress",
  "check_in_time": "2026-02-05T14:00:00Z"
}
```

#### 4.2 下岗打卡(兼职者)

**功能描述**: 兼职者完成工作后打卡

**前置条件**:
- 已登录
- 角色为兼职者
- 报名状态为 `in_progress`

**业务规则**:
1. 校验是否已上岗打卡
2. 更新报名状态为 `completed`
3. 记录 `check_out_time` = 当前时间
4. 计算工作时长

**输出**:
```json
{
  "application_id": "app_123",
  "status": "completed",
  "check_out_time": "2026-02-05T18:00:00Z",
  "work_hours": 4
}
```

---

### 5. 结算模块

#### 5.1 确认完工(招聘方)

**功能描述**: 招聘方确认兼职者完成工作

**前置条件**:
- 已登录
- 角色为招聘方
- 报名状态为 `completed`

**业务规则**:
1. 校验是否为该岗位的招聘方
2. 更新报名状态为 `settled`
3. 自动结算:
   - 计算薪资: `salary = hourly_rate * work_hours`
   - 发放薪资: 增加兼职者的虚拟余额
   - 退还押金: `frozen_balance -= deposit`, `available_balance += deposit`

**输出**:
```json
{
  "application_id": "app_123",
  "status": "settled",
  "settlement": {
    "salary": 200,
    "deposit_refunded": 20,
    "total": 220
  }
}
```

---

### 6. 我的报名(兼职者)

**功能描述**: 兼职者查看自己的所有报名

**前置条件**:
- 已登录
- 角色为兼职者

**查询参数**:
- 状态筛选

**输出**:
```json
{
  "ongoing": [  // 进行中的报名
    {
      "id": "app_123",
      "job": {
        "title": "超市促销员",
        "salary": 50,
        "location": "北京市朝阳区"
      },
      "status": "accepted",
      "actions": ["check_in"]  // 当前可执行的操作
    }
  ],
  "completed": [  // 已完成的报名
    {
      "id": "app_124",
      "job": {
        "title": "传单派发",
        "salary": 80
      },
      "status": "settled",
      "settlement": {
        "salary": 160,
        "deposit_refunded": 16
      }
    }
  ]
}
```

**操作按钮显示逻辑**:
- `pending`: 显示 "取消报名"
- `accepted`: 显示 "上岗打卡"
- `in_progress`: 显示 "下岗打卡"
- `completed`: 显示 "等待商家确认"
- `settled`: 显示 "已完成"

---

## 状态机设计

### 岗位状态

```
[岗位创建] → open (招聘中)
              ↓
           closed (已关闭/已招满)
```

### 报名状态

```
                    [报名创建]
                         ↓
                    pending (待审核)
                         ↓
              ┌──────────┴──────────┐
              ↓                     ↓
         accepted             rejected (已拒绝)
         (已录用)                    ↓
              ↓                 [自动解冻押金]
         in_progress
         (工作中)
              ↓
         completed
         (已完工)
              ↓
         settled (已结算)
         [自动结算薪资+押金]
```

**状态转换规则**:

| 当前状态 | 可转换状态 | 触发条件 | 操作者 |
|----------|-----------|---------|--------|
| `pending` | `accepted` | 招聘方点击录用 | 招聘方 |
| `pending` | `rejected` | 招聘方点击拒绝 | 招聘方 |
| `pending` | - | 兼职者取消 | 兼职者(v2.0) |
| `accepted` | `in_progress` | 上岗打卡 | 兼职者 |
| `in_progress` | `completed` | 下岗打卡 | 兼职者 |
| `completed` | `settled` | 招聘方确认完工 | 招聘方 |

---

## 数据模型概要

### 核心实体关系

```
User (用户)
  │
  ├─┬─ recruiter (招聘方)
  │ └──> Job (岗位) 1:N
  │       └──> Application (报名) 1:N
  │
  └─┬─ worker (兼职者)
    └──> Application (报名) 1:N
```

### 数据表概览

详细表结构见: `附录/数据库表结构SQL.md`

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `users` | 用户表 | id, phone, role, name, credit_score |
| `jobs` | 岗位表 | id, recruiter_id, title, salary, status |
| `applications` | 报名表 | id, job_id, worker_id, status, check_in_time |

---

## 非功能需求

### 性能要求

- 核心接口响应时间 < 300ms (P95)
- 页面首屏加载 < 2s
- 支持 100 并发用户

### 安全要求

- JWT 认证,Token 有效期 7天
- 密码/敏感信息加密存储
- SQL注入防护
- XSS 防护
- API 限流 (每用户每分钟最多60次请求)

### 可用性要求

- 系统可用性 > 99%
- 数据定时备份(每天凌晨)

### 兼容性要求

- 前端支持: Chrome 90+, Safari 14+, 微信浏览器
- 移动端适配: iOS 13+, Android 8+

---

## 附录

### 术语表

| 术语 | 说明 |
|------|------|
| 押金 | 兼职者报名时冻结的虚拟金额,约为薪资的20% |
| 结算 | 兼职完成后,招聘方发放薪资并退还押金的过程 |
| 鸽子 | 指已报名但未到岗的兼职者 |
| 上岗打卡 | 兼职者到达工作地点后记录到岗时间 |
| 下岗打卡 | 兼职者完成工作后记录离岗时间 |

### 错误码定义

详细错误码见: `附录/错误码定义.md`

| 错误码 | 说明 |
|--------|------|
| `UNAUTHORIZED` | 未登录或Token过期 |
| `FORBIDDEN` | 无权限访问 |
| `NOT_FOUND` | 资源不存在 |
| `INVALID_PARAMS` | 参数错误 |
| `JOB_CLOSED` | 岗位已关闭 |
| `ALREADY_APPLIED` | 已报名过该岗位 |
| `INSUFFICIENT_BALANCE` | 余额不足 |

---

**下一篇: `02-UI UX设计规范.md`** 📐
